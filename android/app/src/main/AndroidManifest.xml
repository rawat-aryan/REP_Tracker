<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Milestone 06: a "health"-type foreground service also needs one of a
         fixed set of data-access permissions granted at runtime, or the OS
         refuses to start it (SecurityException at startForeground). This
         app reads none of the actual health sensors on that list —
         ACTIVITY_RECOGNITION is the closest honest fit for "this is a
         workout timer," not a claim about reading fitness data. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>
    <!-- Fallback type for the window before the user has granted (or has
         denied) ACTIVITY_RECOGNITION — the OS also rejects starting a
         manifest-declared-typed service with type "none" at runtime, so
         the service needs a second declared type it can always legally
         use. dataSync needs only this normal (non-runtime) permission. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <application
        android:label="rep_tracker"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Milestone 02: the trigger layer. See docs/milestones/02-android-trigger-spike.md. -->
        <service
            android:name=".TriggerForegroundService"
            android:exported="false"
            android:foregroundServiceType="health|dataSync" />
        <receiver
            android:name=".TriggerActionReceiver"
            android:exported="false" />
        <service
            android:name=".TriggerQsTileService"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="REP Tracker"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
        </service>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
